# Maintenance scripts

One-off or local-only helpers. Not required for runtime.

| Script | Purpose |
|--------|---------|
| `rewrite-git-emails.sh` | Rewrites all branch commit author/committer emails (run once; requires Git Bash). |

These are intentionally **not** wired into CI. Add new scripts here with a one-line note in this table.